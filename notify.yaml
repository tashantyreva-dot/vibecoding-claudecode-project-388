# Настройки Telegram-уведомлений
#
# ВАЖНО: токен бота сюда НЕ добавляется (это секрет) — он должен храниться
# в переменных окружения на сервере (например, TELEGRAM_BOT_TOKEN), не в git.
# chat_id секретом не является, его можно хранить в репозитории.

telegram:
  chat_id: 1100232251
